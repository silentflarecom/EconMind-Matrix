# Backend Environment Configuration
# Copy this file to .env and customize for your environment

# Environment mode: development or production
ENV=development

# CORS Configuration (comma-separated origins)
# Leave empty in development to allow localhost origins
# In production, set explicit origins:
# CORS_ORIGINS=https://econmind.example.com,https://app.econmind.example.com
CORS_ORIGINS=

# Database path (relative or absolute)
DATABASE_FILE=corpus.db

# API Base URL (for documentation)
API_BASE_URL=http://localhost:8000
